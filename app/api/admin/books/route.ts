import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireApiUser} from "@/lib/auth";import {audit} from "@/lib/audit";export async function GET(){const user=await requireApiUser();if(!user)return NextResponse.json({error:"Não autorizado"},{status:401});const data=await db.bookIngestion.findMany({orderBy:{createdAt:"desc"}});return NextResponse.json({data})}export async function POST(req:Request){const user=await requireApiUser();if(!user)return NextResponse.json({error:"Não autorizado"},{status:401});const body=await req.json();if(!body.fileName||!body.storageUrl||!body.mimeType)return NextResponse.json({error:"Campos obrigatórios ausentes"},{status:400});const book=await db.bookIngestion.create({data:{fileName:body.fileName,storageUrl:body.storageUrl,mimeType:body.mimeType,projectId:body.projectId||null}});await audit("CREATE","BookIngestion",book.id,user.id,{fileName:book.fileName});return NextResponse.json({ok:true,book},{status:201})}
