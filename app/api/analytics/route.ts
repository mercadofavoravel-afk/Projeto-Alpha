import{NextResponse}from"next/server";import{z}from"zod";import{db}from"@/lib/db";const s=z.object({name:z.string().min(2).max(120),path:z.string().max(500).optional(),sessionKey:z.string().max(120).optional(),projectId:z.string().cuid().optional(),profileId:z.string().cuid().optional(),metadata:z.record(z.unknown()).optional()});export async function POST(req:Request){const p=s.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Evento inválido"},{status:400});const e=await db.analyticsEvent.create({data:p.data});return NextResponse.json({ok:true,id:e.id},{status:201})}
