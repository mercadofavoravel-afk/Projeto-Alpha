'use client';import{FormEvent,useState}from"react";import Link from"next/link";type R={id:string;slug:string;name:string;neighborhood:string;description:string;heroImage?:string|null;score:number;reasons:string[]};const bairros=["Ipanema","Leblon","Copacabana","Barra da Tijuca"],tipos=["Studio","Apartamento","Garden","Cobertura","Duplex"];function session(){let k=localStorage.getItem("alpha_session_key");if(!k){k=crypto.randomUUID();localStorage.setItem("alpha_session_key",k)}return k}export function RecommendationQuiz(){const[objective,setObjective]=useState("LIVE"),[bs,setBs]=useState<string[]>([]),[ts,setTs]=useState<string[]>([]),[budget,setBudget]=useState(""),[beach,setBeach]=useState(3),[invest,setInvest]=useState(2),[life,setLife]=useState(3),[results,setResults]=useState<R[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState("");const toggle=(x:string,a:string[],f:(v:string[])=>void)=>f(a.includes(x)?a.filter(i=>i!==x):[...a,x]);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/recommendations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({objective,preferredBairros:bs,preferredTypes:ts,budgetMax:budget?Number(budget):undefined,proximityBeach:beach,proximityMetro:0,investmentFocus:invest,lifestyleFocus:life,sessionKey:session()})});const j=await r.json();if(!r.ok)throw new Error(j.error||"Falha na recomendação");setResults(j.results)}catch(x){setError(x instanceof Error?x.message:"Erro inesperado")}finally{setLoading(false)}}return <><form className="quiz" onSubmit={submit}><div className="quiz-block"><label>Objetivo</label><select value={objective} onChange={e=>setObjective(e.target.value)}><option value="LIVE">Morar</option><option value="INVEST">Investir</option><option value="PATRIMONY">Patrimônio</option></select></div><div className="quiz-block"><label>Bairros preferidos</label><div className="choice-grid">{bairros.map(x=><button type="button" className={bs.includes(x)?"choice active":"choice"} onClick={()=>toggle(x,bs,setBs)} key={x}>{x}</button>)}</div></div><div className="quiz-block"><label>Tipologias</label><div className="choice-grid">{tipos.map(x=><button type="button" className={ts.includes(x)?"choice active":"choice"} onClick={()=>toggle(x,ts,setTs)} key={x}>{x}</button>)}</div></div><div className="quiz-block"><label>Orçamento máximo</label><input value={budget} onChange={e=>setBudget(e.target.value.replace(/\D/g,""))} placeholder="Ex.: 3000000"/></div><div className="quiz-block"><label>Proximidade da praia: {beach}/5</label><input type="range" min="0" max="5" value={beach} onChange={e=>setBeach(Number(e.target.value))}/></div><div className="quiz-block"><label>Foco em investimento: {invest}/5</label><input type="range" min="0" max="5" value={invest} onChange={e=>setInvest(Number(e.target.value))}/></div><div className="quiz-block"><label>Estilo de vida: {life}/5</label><input type="range" min="0" max="5" value={life} onChange={e=>setLife(Number(e.target.value))}/></div><button className="btn" disabled={loading}>{loading?"Analisando...":"Gerar seleção"}</button>{error&&<p className="form-error">{error}</p>}</form>{results.length>0&&<section className="recommendation-results"><div className="head"><div><div className="eyebrow">Seleção personalizada</div><h2>Maior aderência ao perfil.</h2></div><p>Ranking indicativo, sujeito à validação comercial.</p></div><div className="grid">{results.map((p,i)=><article className="card" key={p.id}>{p.heroImage&&<img src={p.heroImage} alt={p.name}/>}<div className="copy"><div className="recommendation-rank">#{i+1} · {p.score} pontos</div><div className="eyebrow">{p.neighborhood}</div><h3>{p.name}</h3><p>{p.description}</p><div className="reason-list">{p.reasons.map(x=><span key={x}>{x}</span>)}</div><Link className="btn" href={`/empreendimentos/${p.slug}`}>Conhecer</Link></div></article>)}</div></section>}</>}
