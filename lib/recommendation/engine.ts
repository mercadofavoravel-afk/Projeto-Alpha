import type{Candidate,Ranked,RecommendationInput}from"./types";
const n=(v:string)=>v.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();
export function rankProjects(projects:Candidate[],input:RecommendationInput):Ranked[]{return projects.map(p=>{let score=0;const reasons:string[]=[];const text=n(`${p.description} ${p.collections.join(" ")} ${p.highlights.join(" ")}`);if(input.preferredBairros.some(x=>n(x)===n(p.neighborhood))){score+=28;reasons.push(`Localização desejada: ${p.neighborhood}`)}const types=input.preferredTypes.filter(x=>p.types.some(t=>n(t)===n(x)));if(types.length){score+=Math.min(20,types.length*10);reasons.push(`Tipologia compatível: ${types.join(", ")}`)}if(input.budgetMax&&p.priceFrom){if(p.priceFrom<=input.budgetMax){score+=18;reasons.push("Faixa de preço compatível")}else if((p.priceFrom-input.budgetMax)/input.budgetMax>.1)score-=20}if(input.areaMin&&p.areaTo&&p.areaTo>=input.areaMin){score+=10;reasons.push("Área compatível")}if(input.bedroomsMin&&p.bedroomsTo&&p.bedroomsTo>=input.bedroomsMin){score+=8;reasons.push("Quartos compatíveis")}if(input.proximityBeach>=3&&/(praia|mar|orla|frente mar)/.test(text)){score+=input.proximityBeach*3;reasons.push("Proximidade da praia")}if(input.investmentFocus>=3&&/(invest|rentabilidade|locacao|short stay|temporada)/.test(text)){score+=input.investmentFocus*3;reasons.push("Aderência a investimento")}if(input.lifestyleFocus>=3&&/(design|arquitetura|exclusiv|bem-estar|lazer)/.test(text)){score+=input.lifestyleFocus*2;reasons.push("Aderência ao estilo de vida")}return{...p,score:Math.max(0,Math.round(score)),reasons:reasons.slice(0,4).length?reasons.slice(0,4):["Correspondência geral com o perfil"]}}).sort((a,b)=>b.score-a.score||a.name.localeCompare(b.name))}
